import { Megaphone } from "lucide-react";
import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function CampaignsPage() {
  return (
    <PlaceholderPage
      title="Campaigns"
      description="Create and track WhatsApp broadcast campaigns."
      icon={Megaphone}
    />
  );
}
